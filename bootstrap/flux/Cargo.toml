[package]
name = "flux"
version = "0.1.0"
authors = ["Adrian Thurston <thurston@colm.net>"]
edition = "2018"

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]
path = "lib.rs"

[dependencies]
core = { path = "core" }

[build-dependencies]
core = { path = "core" }
